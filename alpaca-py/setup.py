from setuptools import setup, find_packages

setup(
    name="alpaca-py",
    version="0.1.0",
    description="A Python project for alpaca-py",
    packages=find_packages(),
    python_requires=">=3.6",
    install_requires=[],
)
